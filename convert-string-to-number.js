// Task: We need a function that can transform a string into a number.

// Parameters: all inputs will be strings. All strings are valid numbers. 
// Return result as string.
// Example: "123" = 123;
// Pseudo code: use a string method to change to numbers.

// my Solution:

const stringToNumber = function(str){
  return (Number(str));
}