// Challenge: given a number, find its opposite (additive inverse). 
// Parameters: number will always be an integer. No other value types.
// Return as a simple function
// Ex. -1 = 1, 15 = -15, 30 = -30
// Pseudo code:
    // take the integer, multiply it by -1


// My solution:

function opposite(number) {
  return -(number);
}